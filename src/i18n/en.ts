import type { Translation } from '@jsverse/transloco';

// English source-of-truth catalog. Keys are populated as components are
// translated (see PLAN-i18n.md for the namespacing scheme). Keep this
// structure mirrored in `de.ts`.
export const en: Translation = {};
